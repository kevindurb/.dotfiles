# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3'

includes:
  homebrew:
    taskfile: ./.config/taskfiles/homebrew
    aliases: [brew, b]
  llm: ./.config/taskfiles/llm

dotenv: [.env]

tasks:
  default: task --list-all
